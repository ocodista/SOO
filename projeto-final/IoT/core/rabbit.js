class Rabbit {
    constructor (connectionString) {
        this.connectionString = connectionString
    }

    connect() {

    }

    consume(queue, action) { }

    pushTo(queue, object) { }
}